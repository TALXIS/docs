Represents a command that can be executed on records.

## Properties

### children

> **children**: `any`[]

Seems to be always empty - buttons present in Flyout and SplitButton are put on first level (the array of buttons is flattened).

#### Defined in

src/utils/dataset/data-providers/interfaces.ts:1419
